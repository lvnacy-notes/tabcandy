---
class: archive
category:
  - changelog
log-scope: general
modified: 2026-08-30
UUID: 6a026efa-4379-4a1f-b158-d50aed7bd53b
commit-sha: "f470e16"
files-modified: 9
files-created: 2
files-archived: 1
tags:
  - tab-candy
---

# Tab Candy Refactor, Part 6: Commands, Bookmarks, and Network Integrations — 2026-08-30

## Overview

| Field | Value |
|-------|-------|
| Date | 2026-08-30 |
| Commit SHA | f470e16828864d6c980b408e0189ec2f86096a1a |
| Files Added | 2 |
| Files Modified | 9 |
| Files Archived | 1 |

## Changes

### Files Modified
- `REFACTOR docs/REFACTOR-DECISIONS.md`: §6 decisions appended (private-registry isolation, bookmark typing, network-request hardening — see Notes).
- `REFACTOR docs/REFACTOR-IMPLEMENTATION-CHECKLIST.md`: §6 items checked off.
- `React/Components/App/App.tsx`: switched to importing bookmarks from `src/services/bookmarks.ts`; the §1 `instanceof TFile` stopgap in the bookmarks render path is now redundant-but-harmless rather than load-bearing, since `getBookmarks()` itself returns `TFile[]`.
- `React/Utils/getQuote.ts`: rewritten so every failure path (network error, non-200 status, malformed body, empty custom-quotes list) returns `null` instead of throwing or returning a malformed object; wrapped in the new `withTimeout()`; fixed a real latent bug where picking a random quote from an empty `customQuotes` array produced a `NaN` index and would have thrown.
- `src/Settings/Settings.ts`: switched to importing bookmarks from `src/services/bookmarks.ts`.
- `src/modals/ChooseSearchProvider.ts`: `getItems()` no longer enumerates `this.app.commands` directly — calls the new `listSearchProviderCommands()` in `commands.ts` instead.
- `src/services/commands.ts`: hardened into the sole file touching `app.commands`/`app.plugins`/`app.internalPlugins` — `isPluginEnabled()` checks both registries privately, and `executeEnabledPluginCommand()` now wraps `executeCommandById()` in `try`/`catch` so a private-registry shape mismatch shows a Notice instead of throwing uncaught.
- `src/services/versionCheck.ts`: wrapped in `try`/`catch` with `throw: false` + manual status checks and the new `withTimeout()`; fixed a real latent false-positive where a failed fetch left the update-available comparison reading as "update available" instead of "unknown."
- `ARCHIVE/CHANGELOG-2026-08-29-0477de2.md` *(renamed from `ARCHIVE/CHANGELOG-2026-08-29.md`)*: disambiguated filename, same-day rename pattern.

### New Files Created
- `src/Utils/withTimeout.ts`: generic `Promise.race`-style timeout wrapper (8s, `REQUEST_TIMEOUT_MS`) — added because `requestUrl()`'s typings expose no `AbortSignal`/timeout option at all, so this is the mechanism actually available given the API's real surface, not a workaround chosen over a supported option that was skipped.
- `src/services/bookmarks.ts`: the sole remaining `internalPlugins` touchpoint in the codebase, replacing the deleted `getBookmarks.ts`. Fully typed (`BookmarkItem`/`BookmarkFileItem`/`BookmarkGroupItem` discriminated union with type guards, no `any`); returns `[]` on any disabled/malformed/missing state at any point in the chain.

### Files Removed / Archived
- `React/Utils/getBookmarks.ts`: deleted outright (not staged or stubbed) in the same pass its replacement landed — an explicit instruction, since file deletion is otherwise reserved for §8.


<!-- archivist:auto-end -->
## Notes

**Commit Message**
chore: refactor, part 6, commands, bookmarks, and network integrations

**Root cause of the broken build**: removing the last @ts-ignores exposed that app.commands, app.plugins, and app.internalPlugins aren't in the pinned obsidian@1.13.1 typings at all — that's exactly §6's territory (commands + bookmarks are the only private-API consumers left in the codebase).
What changed:
- **Commands** — `src/services/commands.ts` is now the only place touching `app.commands`/`app.plugins`. `ChooseSearchProvider.ts` no longer enumerates the command registry itself; it calls the adapter. Execution still shows a Notice and now can't throw uncaught.
- **Bookmarks** — killed `React/Utils/getBookmarks.ts`, built `src/services/bookmarks.ts` as the sole internalPlugins touchpoint, pulling this forward from later in the implementation plan. Fully typed (discriminated union + guards, no more any), returns `[]` on any disabled/malformed/missing state. `getBookmarks()` now correctly returns `TFile[]` instead of the looser `TAbstractFile[]`, which fully closes the §1 typing gap instead of leaving the instanceof stopgap load-bearing.
- **Network** — `versionCheck.ts` and `getQuote.ts` both wrapped in `try`/`catch`, `throw:false` + manual status checks, and a shared `withTimeout.ts`. Fixed a bug along the way: a failed version-check fetch used to read as "update available" instead of "unknown," and an empty custom-quotes list would've thrown.

**Left open**: `switcher:open` as the default search provider wasn't independently re-verified against a live Obsidian runtime — nothing in this section touches it and it's a long-stable id, but "widely believed stable" isn't the same as "verified this session," so it's unchecked rather than assumed. Bookmark-state testing (all-bookmarks, nested/empty groups, missing Bookmarks) is likewise not possible from this sandbox.

**Why `getBookmarks.ts` and `getQuote.ts` landed differently**: both were private-API-adjacent and loosely typed, but only bookmarks got relocated to `src/services/`. §6's own checklist item required consolidating bookmarks' scattered `internalPlugins` access, and REFACTOR.md names that exact destination. Quotes have no equivalent requirement in this section — §7 already separately claims "move quote loading into a service or hook" as its own job, so moving it here would just be doing §7's relocation early for no reason tied to §6's actual scope.

---

*This changelog was automatically generated by Archivist CLI.*
*See [Archivist CLI](https://github.com/lvnacy-notes/archivist-cli) for more information.*